Here is the procedure to install django rest framework.
I will add key points to remember

From documentation: https://www.django-rest-framework.org/
pip install djangorestframework
pip install markdown       # Markdown support for the browsable API.
pip install django-filter  # Filtering support

INSTALLED_APPS = [
    ...
    'rest_framework',
]